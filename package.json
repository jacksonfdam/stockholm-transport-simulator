{
  "name": "stockholm-transport-simulator",
  "version": "1.0.0",
  "description": "Stockholm transport simulator (bus/tram) using static Trafiklab JSONs with OpenAPI-guided importer.",
  "type": "module",
  "main": "presentation/server.js",
  "scripts": {
    "dev": "nodemon --watch presentation --watch application --watch domain --watch infrastructure --watch config --ext js,json --exec \"node scripts/dev-start.js\"",
    "seed": "node scripts/seed-from-trafiklab.js --data ./",
    "seed:routes": "node scripts/seed.js",
    "seed:routes:lines": "node scripts/seed-routes-to-lines.js",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest --runInBand"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "geolib": "^3.3.4",
    "merge-deep": "^3.0.3",
    "mongoose": "^8.5.1",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0",
    "nodemon": "^3.1.0"
  }
}
